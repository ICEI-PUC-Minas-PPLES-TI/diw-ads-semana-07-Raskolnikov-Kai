function openDetailPage(targetPage) {
    if(targetPage === 'hill') {
       window.location = 'detalhes1.html';
       return;
    }
 
    if(targetPage === 'castlevania') {
       window.location = 'detalhes2.html';
       return
    }
 
    if(targetPage === 'nocturne') {
       window.location = 'detalhes3.html';
       return;
    }
 }